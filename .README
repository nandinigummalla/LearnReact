Learning React

npm init
npm i -D parcel
npm i react
npm i react-dom
npm i react-router-dom

npx parcel index.html -- to run the package
npx parcel build index.html -- to build the production ready code

Normal way to create routes with components
const routes = createBrowserRouter([
{ path: "/", element: <App />, errorElement: <Error /> },
{
path: "/about",
element: <About />,
},
{
path: "/contact",
element: <Contact />,
},
]);

two types of routing
client side routing -- all apps will load for first time and for evry call it just load that component
server side routing - reloading the page and getting the page by making network calls

class component lifecycle
https://projects.wojtekmaj.pl/react-lifecycle-methods-diagram/

React Learn
https://react.dev/learn

Adding tailwind

npm install tailwindcss @tailwindcss/postcss

Create a file and add below

.postcssrc

{
"plugins": {
"@tailwindcss/postcss": {}
}
}

Add the bwlow line as below
index.css
@import "tailwindcss";

References:
● https://styled-components.com/
● https://tailwindcss.com/
● https://getbootstrap.com/
● Material UI - https://mui.com/
